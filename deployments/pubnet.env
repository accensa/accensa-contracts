# Deployments are recorded here by deploy.sh after a successful pubnet run.
#
# DO NOT edit this file manually.  It is written automatically by:
#   NETWORK=pubnet TOKEN=<usdc-sac-id> ./deploy.sh
#
# No pubnet deployment has been performed yet.  The values below are
# placeholders that will be replaced by deploy.sh on the first real run.
#
# See docs/MAINNET_DEPLOYMENT.md for the pre-deployment checklist and
# DEPLOYMENTS.md for the human-readable deployment record.

# --- filled by deploy.sh after deployment ---
# Generated by deploy.sh on <timestamp>
# Network: pubnet
# Commit: <sha>
NEXT_PUBLIC_RECEIPT_ANCHOR_ID=<pending-deployment>
NEXT_PUBLIC_REFUND_VAULT_ID=<pending-deployment>
MERCHANT_ADDRESS=<pending-deployment>
TOKEN_ADDRESS=<pending-deployment>
REFUND_WINDOW_LEDGERS=<pending-deployment>
RECEIPT_ANCHOR_VERSION=<pending-deployment>
RECEIPT_ANCHOR_WASM_HASH=<pending-deployment>
REFUND_VAULT_VERSION=<pending-deployment>
REFUND_VAULT_WASM_HASH=<pending-deployment>
