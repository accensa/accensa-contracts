[package]
name = "spike-upto-nested-approve"
version = "0.1.0"
edition = "2021"
publish = false
description = "Research spike: can one Soroban auth entry cover a nested approve? (ADR-002 §6.2)"

[lints]
workspace = true

[lib]
crate-type = ["rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
